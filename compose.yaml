include:
  - services/pihole.yaml
  - services/syncthing.yaml
  - services/jellyfin.yaml
  - services/radicale.yaml
  - network/cloudflared.yaml

secrets:
  cloudflared_token:
    file: secrets/cloudflared_token
